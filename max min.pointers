#include<stdio.h>
int main()
{
  int a[5]={1,2,3,4,5},max,min,*p,i,n=5;
  p=a;
  for(i=0;i<n;i++)
  max=*p;
  min=*p;
  for(i=0;i<n;i++)
  {
    if(*(p+i)>max)
    {  max=*(p+i); }
    if(*(p+i)<min)
    { min=*(p+i);}
  }
  printf(" maximum is %d and minimum is %d",max,min);
  return 0;
}
